# Required for Azure Function Apps
azure-functions

# Form Recognizer SDK (compatible with Python 3.11 and Azure)
azure-ai-formrecognizer==3.3.3

# For accessing blob storage
azure-storage-blob==12.25.1

# Form Recognizer and Azure SDK dependencies
azure-core==1.35.0
azure-common==1.1.28

# HTTP requests (used by Form Recognizer SDK)
requests==2.32.4

# For handling .env files (if used)
python-dotenv==1.1.1

# Optional: PDF file generation if needed in other parts of the app
fpdf==1.7.2
