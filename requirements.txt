# Azure Function runtime
azure-functions

# Azure AI Form Recognizer (stable v3, works with DocumentAnalysisClient)
azure-ai-formrecognizer==3.3.3

# Core Azure SDK packages (must match SDK versions)
azure-core==1.35.0
azure-common==1.1.28

# Azure Blob Storage client
azure-storage-blob==12.25.1

# HTTP library (required by SDKs)
requests==2.32.4

# Load environment variables (optional, useful for local dev)
python-dotenv==1.1.1

# PDF generation library (optional but required if generating PDFs in functions)
fpdf==1.7.2
